# 건설업 사고사례 자동화 시스템 — AI 에이전트 팀 설계서

> **문서 목적**: Claude Code에서 구체적 구현 시 참조할 계획서  
> **버전**: 2.0 | **최초 작성**: 2026-04-25 | **인터뷰 기반 갱신**: 2026-04-25  
> **태그**: KOSHA API · Claude AI 분석 · 자동 PPT 생성 · 웹 대시보드 · 이메일 배포

---

## 목차

1. [작업 컨텍스트](#1-작업-컨텍스트)
2. [워크플로우 정의](#2-워크플로우-정의)
3. [구현 스펙](#3-구현-스펙)
4. [자동화 스케줄링](#4-자동화-스케줄링)
5. [웹 대시보드 설계](#5-웹-대시보드-설계)
6. [이메일 자동 배포 설계](#6-이메일-자동-배포-설계)
7. [운영 비용 계획](#7-운영-비용-계획)
8. [단계별 구현 로드맵](#8-단계별-구현-로드맵)
9. [즉시 시작 가능한 다음 단계](#9-즉시-시작-가능한-다음-단계)
10. [미결정 이슈 및 우려사항](#10-미결정-이슈-및-우려사항)

---

## 1. 작업 컨텍스트

### 1.1 배경 및 목적

건설업 현장의 안전사고 사례를 KOSHA(한국산업안전보건공단) 공식 API와 뉴스 미디어에서 자동 수집하고, Claude AI로 분석하여 PPT 보고서를 자동 생성·배포하는 시스템을 구축한다.  
안전관리자 1인이 수동으로 처리하던 사고사례 수집·분석·보고서 작성 업무를 완전 자동화하는 것이 목표다.

현재 운영 범위는 단독 사용(wjdgns8000@naver.com)이며, 향후 회사 내부 안전관리자 또는 외부 기관으로 배포 범위를 확장할 수 있도록 수신자 관리 구조를 열어둔다.

### 1.2 입출력 정의

| 구분 | 내용 |
|------|------|
| **입력** | 스케줄러 트리거 (주간/월간/긴급), 수집 기간 파라미터, KOSHA serviceKey |
| **최종 출력** | 완성 `.pptx` 보고서 파일, 웹 대시보드 게시, 이메일 자동 발송 |
| **중간 산출물** | 원시 JSON → 분류 JSON → 분석 JSON → 이미지 매핑 JSON → `.pptx` → 검수 리포트 |

### 1.3 제약조건

| 제약 | 내용 |
|------|------|
| API 호출 한도 | KOSHA API 일 1,000건 → 주간 50건, 월간 200건 이내 운영 |
| 저작권 | Google Custom Search 이미지는 CC 필터를 적용하되 완전한 보장이 없음 → 슬라이드에 출처를 반드시 명시. SVG 아이콘(Heroicons 등 라이선스 명확한 라이브러리) 우선 사용 |
| 운영 환경 | 단독 운영(안전관리자 1인), GitHub Actions Cron 기반 서버리스 스케줄 |
| 비용 | 월 최대 1만 원 수준 유지 (Claude API + 호스팅). 현재 Claude Code $20 정액권 사용 중이며, 프로덕션 자동화 시 별도 API 키 필요 여부는 미결정 |

### 1.4 용어 정의

| 용어 | 정의 |
|------|------|
| KOSHA | 한국산업안전보건공단. 건설업 재해사례 공공 API 제공 기관 |
| 재해 유형 | 추락, 붕괴, 낙하물, 감전, 끼임, 화재 등 사고 분류 코드 |
| 1사고 1슬라이드 | PPT 조립 원칙 — 사고 1건당 슬라이드 1장으로 구성. 단, 유형별 섹션 타이틀 슬라이드를 별도로 삽입하여 가독성 확보 (레이아웃 확정은 §10 참조) |
| 신뢰도 점수 | 수집 데이터의 정확성 점수 (0~100). 60점 미만 시 플래그 처리. 초기값 60은 실제 KOSHA 데이터로 실험 후 보정 예정 |
| 오케스트레이터 | 전체 에이전트 실행 순서와 데이터 흐름을 조율하는 총괄 스케줄러 |

---

## 2. 워크플로우 정의

### 2.1 전체 흐름

```
[스케줄러 트리거] — GitHub Actions Cron (정기) / RSS 30분 폴링 (긴급)
        │
        ▼
[실행 전 준비]
  /output/YYYYMMDD/ 폴더 클린업 후 재생성 (이전 실패 파일 오염 방지)
        │
        ▼
[Agent 1: 수집]
  KOSHA API(우선) + 뉴스 RSS 수집(보완)
  board_no 기준 중복 제거
  출력: 원시 사고데이터 JSON
        │
        ▼
[Agent 2: 검토·분류]
  정확성 검토 + 유형 분류 + 목차 생성
  신뢰도 60점 미만 → 플래그 처리
  출력: 분류 JSON + 목차 데이터
        │
        ▼
[Agent 3: 분석] ← 사고 1건씩 개별 호출 (컨텍스트 안정성 확보)
  Claude AI로 발생경위·원인·인사이트·예방대책 구조화
  출력: 구조화 분석 JSON
        │
        ▼
[Agent 4: 시각화]
  SVG 아이콘 우선 + Google Custom Search CC 이미지 보완
  이미지마다 출처(image_credit) 필드 필수 기록
  출력: 이미지 경로 포함 JSON
        │
        ▼
[Agent 5: PPT 조립]
  슬라이드 생성 (유형별 섹션 타이틀 + 1사고 1슬라이드)
  출력: 완성 .pptx 파일
        │
        ▼
[Agent 6: 검수]
  정합성·누락·출처·오탈자 점검
  통과 → 승인 신호 / 반려 → Agent 5 재실행 (최대 2회)
        │
        ▼
[Agent 7: 배포]
  웹 대시보드 업로드 + 이메일 발송
  출력: 게시 완료 + 발송 로그
```

### 2.2 단계별 성공 기준 및 검증

| 에이전트 | 성공 기준 | 검증 방법 | 실패 시 처리 |
|----------|----------|----------|-------------|
| **Agent 1 수집** | 사고 데이터 1건 이상 수집, 필수 필드(사고일자·장소·경위) 존재 | 스키마 검증 | 자동 재시도 (최대 3회) → 실패 시 관리자 알림 |
| **Agent 2 분류** | 신뢰도 60점 이상 항목 전체 분류 완료, 목차 데이터 생성 | 규칙 기반 + LLM 자기 검증 | 신뢰도 60점 미만 → 플래그 후 Agent 6에 추가 검토 위임 |
| **Agent 3 분석** | 사고경위(200자 이내), 원인 3개, 인사이트 3개, 예방대책 3개, 관련 법령 포함 | 스키마 검증 + 규칙 기반 | 자동 재시도 (최대 2회) → 항목 스킵 + 로그 기록 |
| **Agent 4 시각화** | 슬라이드별 이미지 또는 아이콘 최소 1개 매핑, 출처 필드 존재 | 스키마 검증 | SVG 아이콘으로 대체, 스킵 금지 |
| **Agent 5 PPT 조립** | 표지·목차·유형섹션·사고사례·인사이트·출처 슬라이드 전체 포함, 빈 슬라이드 없음 | 규칙 기반 | 자동 재시도 (최대 2회) → 반려 시 관리자 에스컬레이션 |
| **Agent 6 검수** | 슬라이드 수 일치, 출처 전체 표기, 원인 3개 이상, 빈 슬라이드 없음 | 규칙 기반 + LLM 자기 검증 | 반려 시 Agent 5 재실행 요청 (최대 2회) → 오류 목록 이메일 발송 후 관리자 수동 판단 |
| **Agent 7 배포** | 웹 게시 완료 + 이메일 발송 성공 로그 기록 | 규칙 기반 | 이메일 발송 실패 시 재시도 (최대 3회) → 관리자 알림 이메일 |

### 2.3 LLM 판단 영역 vs 코드 처리 영역

| LLM(Claude)이 수행 | 스크립트가 처리 |
|--------------------|----------------|
| 재해 유형 분류 (Agent 2) | KOSHA API 호출 및 응답 파싱 (Agent 1) |
| 신뢰도 점수 판단 (Agent 2) | TF-IDF 유사도 중복 검사 (Agent 2) |
| 경위·원인·인사이트·예방대책 작성 (Agent 3) | 날짜·장소 형식 유효성 검증 (Agent 2) |
| 사고 유형별 이미지 키워드 결정 (Agent 4) | Google Custom Search API 호출 (Agent 4) |
| 정합성 검수 판단 (Agent 6) | 슬라이드 텍스트 추출 및 항목 수 계산 (Agent 6) |
| 이메일 본문 요약 생성 (Agent 7) | SMTP 이메일 발송 (Agent 7) |

### 2.4 데이터 소스 통합 전략

뉴스와 KOSHA 공식 데이터는 내용이 다를 수 있음(사고 규모 과장, 오보 등)이 가장 큰 기술적 우려 사항이다.

| 단계 | 처리 방식 |
|------|----------|
| 수집 | KOSHA API 먼저 수집 → Naver 뉴스 RSS 수집 (보완) |
| 중복 감지 | `board_no` 기준 1차 매칭 + TF-IDF 유사도 기준 2차 매칭 |
| 충돌 해소 | 동일 사고로 판단 시 KOSHA 공식 데이터로 덮어쓰기. 뉴스 URL은 `source_url`에 별도 보존 |
| 뉴스 단독 사례 | KOSHA에 없는 뉴스 전용 사고는 신뢰도 점수를 낮게 설정(< 70)하고 플래그 |

---

## 3. 구현 스펙

### 3.1 폴더 구조

```
/construction-safety-agent
  ├── CLAUDE.md                          # 오케스트레이터 지침 (메인)
  ├── /.claude
  │   ├── /agents
  │   │   ├── collector/AGENT.md         # Agent 1: 수집
  │   │   ├── classifier/AGENT.md        # Agent 2: 검토·분류
  │   │   ├── analyst/AGENT.md           # Agent 3: 분석
  │   │   ├── visualizer/AGENT.md        # Agent 4: 시각화
  │   │   ├── ppt-builder/AGENT.md       # Agent 5: PPT 조립
  │   │   ├── qa-inspector/AGENT.md      # Agent 6: 검수
  │   │   └── publisher/AGENT.md         # Agent 7: 배포
  │   └── /skills
  │       ├── kosha-api/
  │       │   ├── SKILL.md
  │       │   ├── /scripts               # KOSHA API 호출, 응답 파싱
  │       │   └── /references            # API 가이드, 필드 정의
  │       ├── naver-news/
  │       │   ├── SKILL.md
  │       │   └── /scripts               # Naver Search API 호출, RSS 파싱
  │       ├── claude-analyzer/
  │       │   ├── SKILL.md
  │       │   └── /references            # 분석 프롬프트 템플릿, Few-shot 예시
  │       ├── image-mapper/
  │       │   ├── SKILL.md
  │       │   └── /scripts               # Google Custom Search, 아이콘 매핑
  │       ├── pptx-builder/
  │       │   ├── SKILL.md
  │       │   └── /scripts               # Node.js + pptxgenjs 조립 스크립트
  │       ├── pptx-extractor/
  │       │   ├── SKILL.md
  │       │   └── /scripts               # python-pptx 텍스트 추출
  │       ├── email-sender/
  │       │   ├── SKILL.md
  │       │   └── /scripts               # SMTP 발송, 재시도 로직
  │       └── web-uploader/
  │           ├── SKILL.md
  │           └── /scripts               # FastAPI 업로드 API 호출
  ├── .github/
  │   └── workflows/
  │       └── schedule.yml               # GitHub Actions Cron 스케줄러
  ├── /output                            # 날짜별 산출물 (Railway ephemeral → Google Drive 동기화)
  │   ├── /raw                           # Agent 1 원시 JSON
  │   ├── /classified                    # Agent 2 분류 JSON
  │   ├── /analyzed                      # Agent 3 분석 JSON
  │   ├── /visualized                    # Agent 4 이미지 경로 JSON
  │   ├── /pptx                          # Agent 5 완성 .pptx
  │   ├── /qa-reports                    # Agent 6 검수 리포트
  │   └── /logs                          # 전체 실행 로그
  ├── /web                               # FastAPI 웹 대시보드
  │   ├── main.py
  │   ├── /templates
  │   └── /static
  └── /docs
      ├── kosha-api-guide.md
      └── accident-type-taxonomy.md
```

### 3.2 CLAUDE.md 핵심 섹션 목록

- 역할 정의: 오케스트레이터 — 전체 에이전트 순서 조율
- 실행 모드: 주간/월간/긴급 구분 및 파라미터 처리
- **실행 전 클린업**: `/output/YYYYMMDD/` 폴더를 실행 시작 시점에 삭제 후 재생성 — 이전 실패 파일로 인한 오염 방지
- 에이전트 호출 순서 및 데이터 전달 경로 (`/output/` 파일 기반)
- 실패 처리 정책: 재시도 횟수, 에스컬레이션 조건
- 환경 변수 목록: `KOSHA_API_KEY`, `CLAUDE_API_KEY`, `NAVER_API_KEY`, `GMAIL_SMTP_*`, `GOOGLE_DRIVE_*`

### 3.3 에이전트 구조

**서브에이전트 분리 방식 채택** — 7개 에이전트가 역할별로 독립 운영되며, CLAUDE.md(오케스트레이터)가 순차 조율한다.

분리 이유:
- 각 에이전트가 상이한 도메인 지식과 프롬프트를 요구함 (수집 ≠ 분석 ≠ PPT 조립)
- 특정 에이전트 실패 시 해당 단계만 재시도 가능 → 전체 파이프라인 재실행 방지 (파일 기반 중간 산출물로 재개점 명확)
- 컨텍스트 윈도우 최적화: 필요 시점에만 해당 AGENT.md 로드

**에이전트 간 통신 원칙**: 서브에이전트 간 직접 호출 금지 → 오케스트레이터(CLAUDE.md)를 통해 조율. 파일 기반 통신은 Claude Code 서브에이전트 표준 패턴이며, 재개 가능성과 디버깅 투명성을 동시에 확보한다.

### 3.4 서브에이전트 상세

| 에이전트 | 역할 | 입력 | 출력 | 참조 스킬 |
|----------|------|------|------|----------|
| `collector` | KOSHA API + 뉴스 수집, 중복 제거 | 스케줄 파라미터 | `/output/raw/YYYYMMDD.json` | `kosha-api`, `naver-news` |
| `classifier` | 정확성 검토, 유형 분류, 목차 생성 | raw JSON 경로 | `/output/classified/YYYYMMDD.json` | `claude-analyzer` |
| `analyst` | 경위·원인·인사이트·예방대책 구조화 **(사고 1건씩 개별 호출)** | classified JSON 경로 | `/output/analyzed/YYYYMMDD.json` | `claude-analyzer` |
| `visualizer` | SVG 아이콘 우선 + Google CC 이미지 매핑 | analyzed JSON 경로 | `/output/visualized/YYYYMMDD.json` | `image-mapper` |
| `ppt-builder` | 슬라이드 조립, .pptx 파일 생성 | visualized JSON 경로 + 목차 데이터 | `/output/pptx/YYYYMMDD.pptx` | `pptx-builder` |
| `qa-inspector` | 정합성 검수, 배포 승인 여부 결정 | .pptx 경로 + analyzed JSON | `/output/qa-reports/YYYYMMDD.json` + 승인 신호 | `pptx-extractor`, `claude-analyzer` |
| `publisher` | 웹 업로드 + 이메일 발송 | .pptx 경로 + 승인 신호 | 게시 URL + `/output/logs/send_YYYYMMDD.log` | `web-uploader`, `email-sender` |

### 3.5 스킬 목록

| 스킬 | 역할 | 트리거 조건 |
|------|------|------------|
| `kosha-api` | KOSHA 재해사례 API 호출 및 응답 파싱 | `collector` 에이전트 실행 시 |
| `naver-news` | Naver Search API 뉴스 수집 및 RSS 파싱 | `collector` 에이전트 실행 시 |
| `claude-analyzer` | Claude API 호출 (분류·분석·검수 프롬프트) | `classifier`, `analyst`, `qa-inspector` 실행 시 |
| `image-mapper` | Google Custom Search + SVG 아이콘 매핑 | `visualizer` 에이전트 실행 시 |
| `pptx-builder` | Node.js + pptxgenjs 슬라이드 조립 (레이아웃 품질 우선) | `ppt-builder` 에이전트 실행 시 |
| `pptx-extractor` | python-pptx 텍스트 추출 및 항목 수 계산 | `qa-inspector` 에이전트 실행 시 |
| `email-sender` | Gmail SMTP 이메일 발송 및 재시도 | `publisher` 에이전트 실행 시 |
| `web-uploader` | FastAPI 업로드 API 호출, 게시 확인 | `publisher` 에이전트 실행 시 |

> **pptx-builder 런타임 참고**: 나머지 스택이 Python이나 pptxgenjs의 레이아웃 품질 우위로 Node.js를 유지한다. Railway 배포 시 Dockerfile에 Node.js + Python 이중 런타임을 설정하고, Python 코드에서 `subprocess`로 Node 스크립트를 호출한다.

### 3.6 주요 산출물 파일 형식

| 파일 | 위치 | 형식 | 핵심 필드 |
|------|------|------|----------|
| 원시 데이터 | `/output/raw/YYYYMMDD.json` | JSON Array | `accident_date`, `location`, `description`, `source_url`, `board_no` |
| 분류 데이터 | `/output/classified/YYYYMMDD.json` | JSON Array | `type` (추락 등), `confidence_score`, `toc_order` |
| 분석 데이터 | `/output/analyzed/YYYYMMDD.json` | JSON Array | `summary_200`, `causes[3]`, `insights[3]`, `preventions[3]`, `law_refs` |
| 이미지 매핑 | `/output/visualized/YYYYMMDD.json` | JSON Array | `image_url`, `image_credit`, `icon_path`, `slide_layout` |
| PPT 파일 | `/output/pptx/YYYYMMDD.pptx` | Binary | 표지·목차·유형섹션·사례N장·인사이트·출처 구성 |
| 검수 리포트 | `/output/qa-reports/YYYYMMDD.json` | JSON Object | `status` (pass/fail), `errors[]`, `approved_at` |
| 발송 로그 | `/output/logs/send_YYYYMMDD.log` | Plain Text | 수신자별 발송 결과, 타임스탬프 |

### 3.7 파이프라인 안정성 정책

| 정책 | 내용 |
|------|------|
| **실행 전 클린업** | `/output/YYYYMMDD/` 폴더 전체 삭제 후 재생성 → 이전 실패 파일의 오염 재개 방지 |
| **대용량 컨텍스트 분리** | Agent 3은 사고 1건씩 개별 Claude 호출 → 분기/연간(50~70건) 대규모 분석에도 컨텍스트 폭발 없음 |
| **분기·연간 패턴 분석** | 개별 분석 JSON은 이미 완성된 데이터 사용 → 새로운 분석 없이 기존 analyzed JSON에서 통계·패턴만 추출 |
| **Railway 스토리지 영속성** | Railway ephemeral storage는 재배포 시 초기화 → `/output/pptx/`와 SQLite는 Google Drive API로 동기화하여 영속성 확보 |
| **에스컬레이션 흐름** | Agent 6 2회 반려 시: 오류 목록 이메일 발송 → 관리자가 PPT 수동 편집 후 배포 명령 재실행. 또는 오류 감수 후 배포 승인 선택 가능 |

---

## 4. 자동화 스케줄링

**구현 도구**: GitHub Actions Cron — PC 의존성 제거, 클라우드 기반 24시간 자동 실행

> **변경 이유**: 기존 Python APScheduler는 로컬 PC가 켜져 있어야 스케줄이 실행된다. GitHub Actions Cron을 사용하면 PC와 무관하게 서버에서 자동 트리거가 가능하다.

### 4.1 정기 보고서 스케줄

분기·연간 보고서는 운영 범위에서 제외한다. 슬라이드 수 최대 30장으로 고정되어 컨텍스트 관리와 PPT 조립 복잡도가 안정화된다.

| 실행 주기 | GitHub Actions Cron 표현식 | 수집 범위 | 슬라이드 수 (예상) | 특이사항 |
|----------|---------------------------|----------|-----------------|---------|
| 주간 보고서 | `0 23 * * 0` (일요일 UTC 23시 = 월요일 KST 8시) | 최근 7일 | 10~15장 | 속보 위주, 간결 |
| 월간 보고서 | `0 23 L * *` (매월 마지막일 UTC 23시) | 전월 1달 | 20~30장 | 통계 요약 + 유형별 분석 포함 |

### 4.2 긴급 보고서 트리거

긴급 보고서는 KOSHA API가 실시간 웹훅을 제공하지 않으므로 Naver 뉴스 RSS 30분 폴링으로 감지한다.

| 항목 | 내용 |
|------|------|
| 감지 방식 | Naver 뉴스 RSS 30분 간격 폴링 (별도 Actions workflow) |
| 트리거 키워드 | `중대재해`, `건설 사망`, `추락 사고`, `붕괴 사고` |
| 보고서 구성 | 표지 + 사고 1건 상세 + 예방대책 + 출처 (5장 이하) |
| KOSHA 검증 | 뉴스 감지 후 동일 사고를 KOSHA API에서 확인, 공식 데이터 존재 시 교차 검증 후 작성 |
| 기대 지연 | 뉴스 보도 후 최대 30분 이내 감지 (즉시 감지가 아님을 명시) |

---

## 5. 웹 대시보드 설계

### 5.1 페이지 구성

| 페이지 | URL 경로 | 주요 기능 | 인증 요구 |
|--------|---------|---------|---------|
| 메인 대시보드 | `/` | 최신 보고서 카드, 통계 요약, 빠른 다운로드 버튼 | 로그인 후 |
| 보고서 목록 | `/reports` | 주간/월간/분기/연간 탭별 목록, 날짜 필터 | 로그인 후 |
| 보고서 상세 | `/reports/:id` | 슬라이드 미리보기, `.pptx` 다운로드 | 로그인 후 |
| 사고 통계 | `/stats` | 유형별·월별 추이 차트, 지역별 지도 | 로그인 후 |
| 설정 | `/settings` | 이메일 수신자 관리, 수집 키워드, API키 관리 | 로그인 후 |
| 관리자 로그인 | `/login` | 비밀번호 로그인 | 공개 |

**보안 방침**: Railway 서브도메인 URL을 외부에 공유하지 않고, FastAPI 세션 기반 비밀번호 로그인으로 모든 라우팅을 보호한다. 개인 운영 범위에서 이 수준의 보안이면 충분하다고 판단.

### 5.2 메인 대시보드 화면 구성

| 영역 | 상세 내용 |
|------|---------|
| 상단 헤더 | 로고 + "건설안전 사고사례 대시보드" + 로그인 버튼 |
| 통계 카드 4개 | 이달 수집 건수 / 추락 비율 / 최신 수집일 / 다음 보고서 생성일 |
| 최신 보고서 목록 | 카드 형태, 최근 5개 — 제목·날짜·슬라이드 수·다운로드 버튼 |
| 사고 유형 차트 | 도넛 차트 — 이달 추락/붕괴/낙하물/감전/기타 비율 |

### 5.3 기술 스택

| 구분 | 기술 | 이유 |
|------|------|------|
| 백엔드 서버 | Python FastAPI | 가볍고 빠름, 비동기 지원, 학습 용이 |
| 프론트엔드 | HTML + TailwindCSS + Alpine.js | 복잡한 프레임워크 없이 빠르게 구축 가능 |
| 데이터베이스 | SQLite | 설치 불필요, 단독 운영에 충분 |
| 파일 영속 저장 | Google Drive API | Railway ephemeral storage 초기화 대응 — PPT·SQLite를 Drive에 동기화 |
| 이메일 발송 | SMTP (Gmail) 또는 SendGrid | 무료 자동 발송 |
| 서버 호스팅 | Render 무료 플랜 | 월 750시간 무료, 도메인 `project.onrender.com` 제공 — Railway 대비 무료 한도 더 관대 |
| 스케줄러 | GitHub Actions Cron | PC 의존성 없이 클라우드에서 자동 실행, 공개 repo 사용 시 무제한 무료 |

---

## 6. 이메일 자동 배포 설계

| 항목 | 내용 |
|------|------|
| 발송 도구 | Python smtplib + Gmail SMTP (무료) 또는 SendGrid (월 100통 무료) |
| 이메일 제목 | `[건설안전] 2026년 4월 3주차 사고사례 보고서 — 추락 5건, 붕괴 2건` |
| 이메일 본문 | 이달 핵심 통계 요약 + 웹 대시보드 링크 + PPT 직접 첨부 (10MB 이하) |
| 수신자 관리 | 웹 설정 페이지에서 추가/삭제, CSV 일괄 등록 지원 |
| 기본 수신자 | wjdgns8000@naver.com (향후 조직 내 안전관리자로 확장 예정) |
| 발송 실패 처리 | 3회 재시도 후 실패 시 관리자에게 알림 이메일 발송 |

---

## 7. 운영 비용 계획

> **목표**: Claude Code $20 정액권 외 추가 지출 없음. 모든 구성요소를 무료 범위 내에서 운영한다.

### 7.1 자동화 핵심 구조 — Claude Code가 전체 파이프라인 실행

`claude --help`의 `--bare` 옵션 설명:
> *"Anthropic auth is strictly ANTHROPIC_API_KEY or apiKeyHelper. OAuth and keychain are never read."*

즉 **GitHub Actions(Linux 서버)에서는 OAuth 인증이 동작하지 않아 `ANTHROPIC_API_KEY` 없이 Claude를 실행할 수 없다.** API 키를 쓰는 순간 종량제 과금이 발생한다.

따라서 Claude 분석은 GitHub Actions가 아닌 **로컬 PC의 Claude Code CLI**가 담당한다.

```
┌─────────────────────────────────────────────────────────────┐
│ GitHub Actions (무료, 서버에서 자동 실행)                     │
│  • 매주 월요일 08:00: KOSHA API 수집 스크립트 실행             │
│  • 수집된 JSON을 GitHub repo에 커밋                           │
│  • "보고서 생성 준비 완료" 이메일을 사용자에게 발송             │
└─────────────────┬───────────────────────────────────────────┘
                  │ 이메일 수신 후 사용자가 Claude Code 실행
                  ▼
┌─────────────────────────────────────────────────────────────┐
│ 로컬 PC — Claude Code (정액권 $20, 추가 비용 없음)            │
│  claude "주간 보고서 생성해줘"                                 │
│  → Agent 2: 분류·검토       (claude --print 비대화형)         │
│  → Agent 3: 원인·예방대책 분석 (claude --print 비대화형)       │
│  → Agent 4: 이미지 매핑     (SVG 캐시 조회)                  │
│  → Agent 5: PPT 조립        (Node.js subprocess)            │
│  → Agent 6: 검수            (claude --print 비대화형)         │
│  → Agent 7: 이메일 발송 + Render 업로드                       │
└─────────────────────────────────────────────────────────────┘
```

**사용자 실제 업무 흐름 (주당 약 2분)**:
1. 월요일 오전 8시 — 이메일 수신: "이번 주 건설안전 보고서 데이터가 준비되었습니다"
2. Claude Code 열기 → `claude "주간 보고서 생성해줘"` 입력
3. 15~20분 후 자동 완료 — PPT 파일 + 이메일 발송 확인

### 7.2 역할 분담 상세

| 작업 | 실행 주체 | 비용 |
|------|----------|------|
| KOSHA API 데이터 수집 | GitHub Actions (매주 자동) | $0 |
| Naver 뉴스 RSS 수집 | GitHub Actions (매주 자동) | $0 |
| 데이터 준비 완료 알림 이메일 | GitHub Actions | $0 |
| **AI 분류·분석·검수** | **로컬 Claude Code `--print`** | **$0 (정액권 소진)** |
| SVG 아이콘 매핑 | 로컬 Python (캐시 조회) | $0 |
| PPT 파일 조립 | 로컬 Node.js subprocess | $0 |
| 결과 이메일 발송 | 로컬 Python (Gmail SMTP) | $0 |
| 웹 대시보드 파일 업로드 | 로컬 Python → Render API | $0 |

### 7.3 GitHub Actions 역할 — 데이터 수집 + 알림만 담당

```yaml
# .github/workflows/weekly-collect.yml
name: 주간 사고데이터 수집
on:
  schedule:
    - cron: '0 23 * * 0'   # 일요일 UTC 23:00 = 월요일 KST 08:00

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Python 환경 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 의존성 설치
        run: pip install requests

      - name: KOSHA 데이터 수집
        env:
          KOSHA_API_KEY: ${{ secrets.KOSHA_API_KEY }}
        run: python scripts/collect_kosha.py   # Claude 없이 순수 Python

      - name: 수집 결과 커밋
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add output/raw/
          git commit -m "auto: $(date +%Y%m%d) KOSHA 데이터 수집 완료" || echo "변경 없음"
          git push

      - name: 준비 완료 알림 이메일
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASS: ${{ secrets.GMAIL_PASS }}
        run: python scripts/notify_ready.py    # "보고서 생성 준비됨" 이메일 발송
```

**GitHub Actions Secrets에 저장할 값** (Claude 관련 없음):
- `KOSHA_API_KEY` — KOSHA serviceKey
- `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` — 뉴스 RSS용
- `GMAIL_USER`, `GMAIL_PASS` — 알림 이메일용

> `ANTHROPIC_API_KEY`는 GitHub Actions Secrets에 **절대 등록하지 않는다.**

### 7.4 로컬 Claude Code — `claude --print` 비대화형 실행

```bash
# 로컬 PC에서 실행 — $20 정액권 소진, 추가 과금 없음
claude --print --dangerously-skip-permissions \
  "output/raw/20260428.json 파일을 읽고 건설안전 분석을 실행해줘. \
   CLAUDE.md의 지침에 따라 Agent 2부터 7까지 순서대로 실행해줘."
```

Claude Code는 CLAUDE.md를 읽고 자동으로:
- Agent 2 분류 → `output/classified/*.json` 저장
- Agent 3 분석 → `output/analyzed/*.json` 저장  
- Agent 4~7 순차 실행 → PPT 생성 + 이메일 발송까지 완료

### 7.5 서비스별 무료 한도 및 초과 방지

| 서비스 | 무료 한도 | 실제 사용량 | 초과 방지 방법 |
|--------|----------|-----------|--------------|
| **KOSHA API** | 일 1,000건 (영구 무료) | 주 50건 | 방지 불필요 |
| **Naver 검색 API** | 일 25,000건 (영구 무료) | 주 100건 | 방지 불필요 |
| **Gmail SMTP** | 일 500통 (영구 무료) | 주 1~3통 | 방지 불필요 |
| **Google Drive** | 15GB 무료 | 월 32MB | 방지 불필요 (38년치 여유) |
| **GitHub Actions** | 공개 repo 무제한 무료 | 주 1회 × ~5분 = 20분/월 | **repo를 Public으로 설정** |
| **Render** | 월 750시간 무료 | 월 744시간 | 인스턴스 1개만 유지 |
| **Google Custom Search** | 일 100건 무료 | 주 0~5건 (캐싱) | SVG 아이콘 우선 + `image_cache.json` |

### 7.6 비용이 발생하는 4가지 시점과 차단법

| 시점 | 원인 | 차단 방법 |
|------|------|---------|
| 코드에 `ANTHROPIC_API_KEY` 설정 | anthropic SDK가 API 키 감지 시 자동 종량제 전환 | `.env`에 해당 키 없음 유지, GitHub Secrets에도 등록 금지 |
| GitHub Actions에서 `claude` CLI 실행 시도 | Actions 환경(Linux)은 OAuth 불가 → API 키 요구 | Actions는 데이터 수집만 담당, Claude 실행은 로컬만 |
| Google Custom Search 대량 호출 | 일 100건 초과 시 1,000건당 $5 | `image_cache.json` 캐싱 필수 구현 |
| Render 인스턴스 2개 이상 실행 | 무료는 1개만 | 웹 대시보드 인스턴스 1개 유지 |

### 7.7 최종 비용 구조

```
월 지출:
  Claude Code 정액권  $20.00  ← 이것만 지불
  그 외 모든 서비스    $0.00  ← 무료 범위 유지
  ─────────────────────────
  합계                $20.00
```

---

## 8. 단계별 구현 로드맵

### Phase 1 — 핵심 파이프라인 구축 (2~3주)

**목표**: KOSHA API → Claude 분석 → PPT 자동생성까지 1회 완전 실행

- [ ] KOSHA API 수집 스크립트 완성 (Agent 1) — API 연결 완료 ✅, 필드 품질 상세 검증 필요
- [ ] Claude API 분석 프롬프트 설계 및 JSON 출력 확인 (Agent 2, 3)
- [ ] PPT 조립 스크립트 완성 및 샘플 PPT 생성 (Agent 5) — 샘플 완성 ✅
- [ ] **PPT 슬라이드 레이아웃 확정** — 샘플 PPT 리뷰 후 유형 섹션 구조 결정
- [ ] 전체 파이프라인 1회 수동 실행 테스트

### Phase 2 — 자동화 스케줄링 (1~2주)

**목표**: 주간 보고서 자동 생성 및 저장 자동화

- [ ] GitHub Actions Cron 주간 자동 실행 설정
- [ ] 시각화 에이전트 연동 (SVG 아이콘 자동 삽입) (Agent 4)
- [ ] 검수 에이전트 기본 체크리스트 구현 (Agent 6)
- [ ] Google Drive API 연동 — 산출물 영속 저장

### Phase 3 — 웹 대시보드 구축 (2~4주)

**목표**: 웹사이트 오픈 및 PPT 온라인 조회 가능

- [ ] FastAPI 백엔드 기본 구조 구현
- [ ] 메인 대시보드 + 보고서 목록 페이지 구현
- [ ] Railway에 서버 배포 (Node.js + Python 이중 런타임 Dockerfile)
- [ ] PPT 파일 업로드 + 다운로드 기능 연동 (Agent 7)

### Phase 4 — 이메일 배포 + 고도화 (1~2주)

**목표**: 완전 자동화 완성 — 실행하지 않아도 매주 이메일 수신

- [ ] Gmail SMTP 이메일 자동 발송 구현
- [ ] Naver RSS 30분 폴링 긴급 감지 워크플로우 추가
- [ ] 월간 보고서 스케줄 추가 (분기·연간 제외)
- [ ] 사고 통계 차트 페이지 구현
- [ ] 수신자 관리 설정 페이지 구현

---

## 9. 즉시 시작 가능한 다음 단계

| 순위 | 상태 | 작업 |
|------|------|------|
| — | ✅ 완료 | KOSHA API 연결 및 serviceKey 발급 (2026-04-25) |
| — | ✅ 완료 | PPT 샘플 생성 — pptxgenjs 레이아웃 검증 |
| 1순위 | 🔜 진행 | PPT 샘플 리뷰 → 슬라이드 레이아웃 확정 (유형 섹션 구조 결정) |
| 2순위 | 🔜 진행 | Claude API 분석 프롬프트 작성 → KOSHA 데이터 자동 분석 연동 |
| 3순위 | 🔜 대기 | GitHub Actions Cron 주간 자동 실행 스크립트 작성 |
| 4순위 | 🔜 대기 | Google Drive API 연동 (Railway ephemeral storage 대응) |
| 5순위 | 🔜 대기 | FastAPI 기반 웹 대시보드 기본 페이지 구축 |
| 6순위 | 🔜 대기 | Gmail SMTP 이메일 자동 발송 연결 |

---

## 10. 미결정 이슈 및 우려사항

### 10.1 미결정 이슈

| 이슈 | 현황 | 결정 시점 |
|------|------|----------|
| **Claude 실행 방식** | ✅ 확정: 로컬 PC에서 `claude --print` 실행 (정액권 소진). GitHub Actions는 데이터 수집·알림만 담당. `ANTHROPIC_API_KEY` 사용 금지 | 확정 완료 |
| **PPT 슬라이드 레이아웃** | 1사고 1슬라이드 + 유형 섹션 구조 확정 전 — 실제 샘플 보고 결정 | Phase 1 샘플 리뷰 시 |
| **Agent 6 에스컬레이션 후속 흐름** | 2회 반려 시 오류 이메일 발송 방향 정함, 세부 UI 플로우(재배포 명령 방식) 미결 | Phase 2 구현 시 |
| **Railway ephemeral storage 해결** | Google Drive API 동기화 방향 확정, 구체적 구현 방식 미결 | Phase 2 배포 시 |
| **이미지 저작권 집행 방법론** | Google CC 필터 + 출처 명시 방향 확정, SVG 아이콘 라이브러리 선정 미결 | Phase 2 시각화 구현 시 |
| **대용량 컨텍스트 관리** | ✅ 해소: 분기·연간 보고서 제외로 최대 슬라이드 30장 고정 — 건별 호출로 충분 | 확정 완료 |

### 10.2 핵심 기술적 우려사항

| 우려 | 내용 | 완화 방안 |
|------|------|----------|
| **데이터 정확성** | 뉴스 수집 사고가 KOSHA 공식 데이터와 내용이 다를 수 있음 (가장 큰 우려) | KOSHA 데이터 우선, 충돌 시 덮어쓰기 + 뉴스 단독 사례 신뢰도 점수 하향 |
| **LLM 분석 일관성** | 동일 데이터도 프롬프트 워딩에 따라 분류·점수가 달라질 수 있음 | Few-shot 예시 고정 + KOSHA 실데이터로 임계값 실험 보정 |
| **PPT 시각적 품질** | 자동 생성 PPT는 수작업 PPT 대비 디자인 한계 | pptxgenjs 레이아웃 템플릿 사전 설계로 품질 기준선 확보 |
| **Railway 재배포 데이터 소실** | 무료 플랜 ephemeral storage는 재배포 시 초기화 | Google Drive API로 외부 동기화 필수 |
| **GitHub Actions Claude 실행 방식** | ✅ 확정: Actions에서 Claude 실행 안 함 — Linux 환경에서 OAuth 불가(--bare 옵션 제약), API 키 없이 실행 불가능 | 확정 완료 |

---

## 전체 기술 스택 요약

| 컴포넌트 | 기술/라이브러리 | 역할 |
|---------|----------------|------|
| 자동 수집 스케줄러 | GitHub Actions Cron (공개 repo → 무료 무제한) | 매주 월요일 KOSHA 데이터 수집 + 준비 완료 알림 이메일 |
| 오케스트레이터 | 로컬 CLAUDE.md + `claude --print` | 사용자가 명령 실행 시 전체 파이프라인 순차 조율 |
| 수집 (Agent 1) | requests + KOSHA API + Naver RSS — GitHub Actions에서 실행 | KOSHA 재해사례(우선) + 뉴스(보완) 수집, 모두 무료 |
| 분류·분석 (Agent 2, 3) | 로컬 `claude --print` CLI (정액권 소진, API 비용 없음) | 데이터 검토, 분류, 원인분석, 인사이트 |
| 시각화 (Agent 4) | Heroicons SVG 우선 + `image_cache.json` + Google Custom Search(일 100건 무료) | 이미지 매핑, 캐싱으로 외부 호출 최소화 |
| PPT 생성 (Agent 5) | 로컬 Node.js + pptxgenjs (Python subprocess 호출) | 슬라이드 조립 및 .pptx 파일 생성 |
| 검수 (Agent 6) | 로컬 `claude --print` CLI + python-pptx | 정합성 자동 검증 |
| 파일 영속 저장 | Google Drive API (15GB 무료) | Render 재배포 시 산출물 보존 |
| 웹 대시보드 | FastAPI + HTML/TailwindCSS + SQLite | 보고서 조회 및 관리 웹사이트 |
| 이메일 배포 (Agent 7) | 로컬 smtplib (Gmail SMTP, 일 500통 무료) | 자동 이메일 발송 |
| 서버 호스팅 | Render 무료 플랜 (이중 런타임 Dockerfile) | 월 750시간 무료, `project.onrender.com` 서브도메인 |
| Secrets 관리 | GitHub Actions Secrets + 로컬 .env | KOSHA키, Naver키, Drive키, Gmail키 (`ANTHROPIC_API_KEY` 없음) |

---

*이 설계서는 Claude Code 구현 시 참조용 계획서입니다. 각 AGENT.md, SKILL.md의 상세 내용은 구현 단계에서 작성합니다.*  
*v2.0 — 인터뷰 기반 갱신: 스케줄러 변경(APScheduler→GitHub Actions), 긴급 감지 방법 명시, Railway ephemeral storage 이슈 추가, 데이터 소스 통합 전략 추가, 미결정 이슈 섹션 신설*  
*v2.1 — 비용 구조 전면 재설계: GitHub Actions의 Claude CLI OAuth 제약 확인(--bare 옵션), Actions는 데이터 수집+알림만 담당, Claude 실행은 로컬 전용으로 확정. $20 정액권 외 추가 비용 $0 구조 완성*
